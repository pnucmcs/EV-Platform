apiVersion: v1
kind: Service
metadata:
  name: {{ include "stationservice.fullname" . }}-api
  labels:
    {{- include "stationservice.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "stationservice.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
