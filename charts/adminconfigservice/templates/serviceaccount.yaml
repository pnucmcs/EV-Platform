{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "adminconfigservice.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "adminconfigservice.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
