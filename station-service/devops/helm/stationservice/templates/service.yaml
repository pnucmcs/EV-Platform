apiVersion: v1
kind: Service
metadata:
  name: {{ include "stationservice.fullname" . }}
  labels:
    {{- include "stationservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stationservice.selectorLabels" . | nindent 4 }}
