apiVersion: v1
kind: Service
metadata:
  name: {{ include "reportingservice.fullname" . }}
  labels:
    {{- include "reportingservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reportingservice.selectorLabels" . | nindent 4 }}
