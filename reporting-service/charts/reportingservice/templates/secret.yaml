apiVersion: v1
kind: Secret
metadata:
  name: {{ include "reportingservice.fullname" . }}-secret
type: Opaque
stringData:
  ConnectionStrings__Postgres: {{ .Values.env.ConnectionStrings__Postgres | quote }}
