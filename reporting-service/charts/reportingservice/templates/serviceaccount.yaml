{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "reportingservice.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "reportingservice.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
