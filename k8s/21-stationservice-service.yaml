apiVersion: v1
kind: Service
metadata:
  name: stationservice-api
  namespace: ev-platform-dev
spec:
  type: ClusterIP
  selector:
    app: stationservice
  ports:
    - name: http
      port: 80
      targetPort: 8080
