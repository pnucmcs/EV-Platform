{{- if .Values.connectionStrings.postgres }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "notificationservice.fullname" . }}-secret
  labels:
    {{- include "notificationservice.labels" . | nindent 4 }}
type: Opaque
stringData:
  ConnectionStrings__Postgres: {{ .Values.connectionStrings.postgres | quote }}
{{- end }}
