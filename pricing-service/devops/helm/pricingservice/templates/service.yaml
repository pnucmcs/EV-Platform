apiVersion: v1
kind: Service
metadata:
  name: {{ include "pricingservice.fullname" . }}
  labels:
    {{- include "pricingservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pricingservice.selectorLabels" . | nindent 4 }}
