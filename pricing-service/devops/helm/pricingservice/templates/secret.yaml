{{- if .Values.connectionStrings.postgres }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pricingservice.fullname" . }}-secret
  labels:
    {{- include "pricingservice.labels" . | nindent 4 }}
type: Opaque
stringData:
  ConnectionStrings__Postgres: {{ .Values.connectionStrings.postgres | quote }}
{{- end }}
