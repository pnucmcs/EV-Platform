apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pricingservice.fullname" . }}-test-connection"
  labels:
    {{- include "pricingservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pricingservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
