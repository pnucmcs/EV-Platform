{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "telemetryservice.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "telemetryservice.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
