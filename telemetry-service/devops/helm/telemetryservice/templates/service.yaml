apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetryservice.fullname" . }}
  labels:
    {{- include "telemetryservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telemetryservice.selectorLabels" . | nindent 4 }}
